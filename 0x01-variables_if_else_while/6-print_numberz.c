#include <stdlib.h>
#include <stdio.h>
/**
 * main - Prints number between 0 to 9
 * Return: Always 0 (Success)
 */
int main(void)
{
int b;
for (b = 48; b < 58; b++)
{
putchar (b);
}
putchar ('\n');
return (0);
}
